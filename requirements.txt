# Служба, именованный канал, проверка прав вызывающего.
pywin32>=306

# Значок в трее (тянет за собой Pillow — им же рисуется значок).
pystray>=0.19
pillow>=10.0

# Окно на WebView2. Сам движок есть в Windows 10/11 из коробки.
pywebview>=5.0
